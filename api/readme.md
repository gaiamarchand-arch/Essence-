# Essence du Silence — API démo (Vercel)

Routes:
- POST `/api/start` → démarre une session
- POST `/api/turn` → traite une entrée

Après déploiement, mets à jour `servers[0].url` dans `openapi.yaml` avec ton URL Vercel.
